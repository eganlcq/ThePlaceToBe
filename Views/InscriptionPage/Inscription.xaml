<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             BackgroundColor="{StaticResource backgroundColor}"
             x:Class="TPTB.Views.Inscription">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackLayout 
                VerticalOptions="FillAndExpand"  HorizontalOptions="FillAndExpand"
                Grid.Column="0" Grid.Row="0">
                <StackLayout
                    HeightRequest="10">
                <Label Text="ThePlaceToBe" TextColor="{StaticResource textColor}" FontSize="23"
                       Margin="0,5,0,0"
                       HorizontalOptions="Center"/>
                </StackLayout>
            </StackLayout>
            
            <Frame Grid.Column="0" Grid.Row="1"
                   BackgroundColor="Gray"
                   Margin="20,0" Padding="6"
                   HasShadow="False">
                <StackLayout Padding="0,0,0,10"
                             VerticalOptions="FillAndExpand">
                    <StackLayout VerticalOptions="Start" HorizontalOptions="FillAndExpand">
                        <Label Text="Inscription" FontAttributes="Bold" FontSize="15"
                               HorizontalOptions="Center"/>
                    </StackLayout>
                    <StackLayout >
                        <Entry Placeholder="Nom"
                               FontSize="9"/>
                        <Entry Placeholder="Prénom"
                               FontSize="9"/>
                        <Entry Placeholder="Pseudo"
                               FontSize="9"/>
                        <Entry Placeholder="Adresse@email.com"
                               FontSize="9"/>
                        <Entry Placeholder="Mot de passe"
                               FontSize="9"
                               IsPassword="True"/>
                        <Entry Placeholder="Confirmation de mot de passe"
                               FontSize="9"
                               IsPassword="True"/>
                    </StackLayout>
                </StackLayout>
            </Frame>

            <StackLayout Grid.Column="0" Grid.Row="2"
                         HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
                <Button Text="Inscrire"
                        WidthRequest="150"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>